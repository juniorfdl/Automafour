ALTER TABLE EMPRESA
ADD PREFEITURA VARCHAR(100);

ALTER TABLE EMPRESA
ADD SENHA VARCHAR(100);

ALTER TABLE EMPRESA
ADD USER_WEB VARCHAR(100);

ALTER TABLE EMPRESA
ADD AGUARDARCONSULTARETORNO integer;

ALTER TABLE EMPRESA
ADD CONSULTARLOTEAPOSENVIO char(1);

ALTER TABLE EMPRESA
ADD INTERVALOTENTATIVAS integer;

ALTER TABLE EMPRESA
ADD TIPO_RPS char(1);

ALTER TABLE EMPRESA
ADD OPTANTESIMPLESNACIONAL char(1);

ALTER TABLE EMPRESA
ADD INCENTIVADORCULTURAL char(1);
          
CREATE TABLE NOTASERVICO_COMUNICACAO (
    ID INTEGER NOT NULL /* I = INTEGER */,
    ID_NOTASERVICO             INTEGER NOT NULL /* I = INTEGER */,
    DATA_HORA                    TIMESTAMP DEFAULT current_timestamp,
    TIPO                         CHAR(1) NOT NULL /* C1 = CHAR(1) */,
    PROTOCOLO                    VARCHAR(100),
    OBS                          VARCHAR(150),
    XML                          BLOB/* B_TXT = BLOB SUB_TYPE 1 SEGMENT SIZE 80 */,
    CODIGOVERIFICACAO            VARCHAR(20),
    NFSE_NUMERO                  VARCHAR(20)
);

ALTER TABLE NOTASERVICO_COMUNICACAO ADD CONSTRAINT PK_NOTASERVICO_COMUNICACAO PRIMARY KEY (ID);

ALTER TABLE NOTASERVICO_COMUNICACAO ADD CONSTRAINT FK_NOTASERVICO_COMUNICACAO FOREIGN KEY (ID_NOTASERVICO)
 REFERENCES NOTASERVICO (ID) ON DELETE CASCADE ON UPDATE CASCADE;