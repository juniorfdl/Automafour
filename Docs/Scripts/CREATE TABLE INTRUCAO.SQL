CREATE TABLE INSTRUCAO_BANCO (
    CODIGO            VARCHAR(10) NOT NULL,
    DESCRICAO         VARCHAR(100) NOT NULL,
    UTEIS_CORRIDO     CHAR(1),
    GERA_NOSSONUMERO  CHAR(1),
    GERA_BOLETO       CHAR(1),
    BANCA5COD         CHAR(5)
);

ALTER TABLE INSTRUCAO_BANCO ADD CONSTRAINT PK_INSTRUCAO_BANCO PRIMARY KEY (CODIGO);

ALTER TABLE CONTACORRENTE ADD CODIGO VARCHAR(10);
ALTER TABLE CONTACORRENTE ADD DIAS_PROTESTO INTEGER;
