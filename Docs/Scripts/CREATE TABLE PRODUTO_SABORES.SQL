CREATE TABLE PRODUTO_SABORES (
    PRODICOD  INTEGER NOT NULL,
    ID_SABOR  INTEGER NOT NULL,
    PENDENTE  CHAR(1),
    REGISTRO  DATE
);


CREATE TABLE SABORES (
    ID_SABOR   INTEGER NOT NULL,
    DESCRICAO  VARCHAR(100)
);


ALTER TABLE SABORES ADD CONSTRAINT PK_SABORES PRIMARY KEY (ID_SABOR);